day02←{
    out←⊂'Task 1: ',{×/∊+/(2↑⍎)¨'u'⎕R'-'⊢'f'⎕R'0 '⊢('d'~⍨⊃,⊃∘⌽)¨⍵}pget 2021 2 1
    out,←⊂'Task 2: ',f2_2 pget 2021 2 2
    ↑out
}

 res←f2_2 inp;aim;command;depth;horizontal;value
 (horizontal depth aim)←0
 :For (command value) :In (⊃,⍎∘⊃∘⌽)¨inp
     :If command='u'
         aim-←value
     :ElseIf command='d'
         aim+←value
     :ElseIf command='f'
         (horizontal depth)+←value×1 aim
     :EndIf
 :EndFor
 res←horizontal×depth
